/** 롱프레스로 방 액션 메뉴를 열 때 최소 유지 시간 */
export const FRIEND_ROOM_LONG_PRESS_MS = 500;

/**
 * RoomActionModal 퇴장 지연 (JS 타이머).
 * Tailwind `duration-180`과 맞출 것 — 값 변경 시 양쪽 함께 조정.
 */
export const ROOM_ACTION_MODAL_TRANSITION_MS = 180;
